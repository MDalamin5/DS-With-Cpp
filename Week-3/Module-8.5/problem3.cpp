#include<bits/stdc++.h>
using namespace std;
int inversions(vector<int>a);
int main()
{
    int n;
    cin>>n;
    vector<int>a(n);
    for(int i=0;i<n;i++)
    cin>>a[i];
    cout<<inversions(a);
}
int inversions(vector<int>a)
{
    cout<<"work is cooking";
    return 0;
}